#include "main.h"

/**
 * _strlen - function implementation
 *
 * Description: A C program to swap two params
 * @s: the number to swap
 *
 * Return: void
 *
*/

int _strlen(char *s)

{

	int length = 0;

	while (str[length] != '\0')
	{
	length++;
	}

	return (length);

}
